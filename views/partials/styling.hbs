<style type="text/css">
/*@import url('https://fonts.googleapis.com/css2?family=Crimson+Pro&display=swap');*/
/*@import url('https://fonts.googleapis.com/css2?family=Jost&display=swap');*/
:root {
	--default-font: 'Ubuntu', Arial, sans-serif;
	--serif-font: 'Crimson-Pro', serif;
	--default-font-weight: 400;
	--body-bg: #EEF1F3;
	--diffused-blue-heading: #628BA6;
	--diffused-blue-text: #466E88;
}
body {
	font-family: var(--default-font);
	font-weight: var(--default-font-weight);
	overflow-y: scroll;
}
.navbar {
	background-color: whitesmoke;
	border: 1px solid #cecece;
}
.container {
	max-width: 960px;
}
.input-area {
	box-shadow: 0 2px 2px #CDD7DD;
}
.input-area input,
.input-area textarea {
	margin-bottom: 10px;
	resize: none;
}
.input-area textarea:focus {
	box-shadow: none;
}
.hidden {
	display: none;
}
#cta-buttons button {
	min-width: 300px;
}
#search-form {
	padding: 10px 20px;
	background-color: whitesmoke;
}
.center-aligned {
	margin: 0 auto;
}
.table.mapping-table th {
	border-top: 2pt solid #8a8a8a;
	border-bottom: 2pt solid #8a8a8a;
}
.table.mapping-table th,
.table.mapping-table td {
	padding: 0.4rem;
}
.table.mapping-table th:first-child,
.table.mapping-table td:first-child {
	background-color: whitesmoke;
}
.table.mapping-table {
	border-bottom: 2pt solid #8a8a8a;
}
.card.result-card {
	border: none;
}
.card.result-card .card-body {
	padding: 1.25rem 0;
}
</style>